# تشغيل FinnMarket والاستجابة للحوادث

هذا المستند هو مرجع التشغيل المبدئي. مالك الخدمة هو مالك مستودع GitHub ومشروع Supabase، ويجب تسمية بديل بشري قبل الإطلاق التجاري.

## البيئات والنشر

- الإنتاج: فرع `master` وموقع `https://fann1.netlify.app`.
- الاختبار: Deploy Preview لكل Pull Request. لا تُستخدم بيانات مستخدمين حقيقية في المعاينة.
- لا تُدمج تغييرات قاعدة البيانات غير المتوافقة رجوعيًا مع الواجهة المنشورة.
- يجب تطبيق الترحيل المتوافق أولًا، ثم نشر الواجهة، ثم التحقق من المسارات الأساسية.

## المراقبة

- يشغّل GitHub Actions فحص توافر كل 15 دقيقة للرئيسية وملفات SEO والسياسات وSupabase Auth health.
- تُراجع تنبيهات Netlify للنشر وسجلات Supabase لخدمات Auth وDatabase وStorage عند أي عطل.
- يمنع نسخ البريد والجوال ونصوص الرسائل إلى أدوات تتبع الأخطاء. استخدم معرف الطلب والوقت والمسار فقط.
- على المالك تفعيل إشعارات GitHub Actions وNetlify وSupabase لحسابه؛ الكود لا يستطيع فرض إعدادات إشعار الحساب.

## تصنيف الحوادث

- P0: تسريب بيانات، اختراق، فقد بيانات أو توقف كامل. أوقف النشر والتغييرات، اعزل السبب، وابدأ تقييم الإبلاغ النظامي.
- P1: تعطل التسجيل أو النشر أو لوحة الإدارة لعدد كبير. ارجع آخر إصدار آمن وحقق في السجلات.
- P2: خلل جزئي له مسار بديل. افتح Issue وحدد مالكًا وموعدًا.
- P3: تحسين غير معطل. أدرجه في دورة العمل المعتادة.

## التراجع

1. حدّد آخر Deploy ناجح في Netlify وأعد نشره، أو اعكس commit الدمج عبر Pull Request جديد.
2. لا تحذف ترحيل قاعدة بيانات مطبقًا. أنشئ ترحيلًا تعويضيًا يحافظ على البيانات.
3. تحقق بعد التراجع من: الرئيسية، تسجيل الدخول، قراءة إعلان، النشر، الملف الشخصي، الإدارة والبلاغات.
4. وثّق السبب، المدة، الأثر والإجراء الوقائي.

## النسخ الاحتياطي والاستعادة

- الهدف التشغيلي المبدئي: RPO لا يتجاوز 24 ساعة وRTO لا يتجاوز 4 ساعات؛ يلزم التأكد أن خطة Supabase الفعلية تحقق ذلك.
- تحقق شهريًا من حالة النسخ الاحتياطية داخل Supabase ومن صلاحية استعادة قاعدة البيانات.
- صور Storage ليست بديلًا عن نسخة احتياطية. صدّر قائمة الكائنات واختبر استعادة عينة إلى مشروع اختبار معزول كل ثلاثة أشهر.
- لا تُجرّب الاستعادة فوق الإنتاج. سجّل تاريخ الاختبار، مدته، النتيجة وأي بيانات تعذر استعادتها.

## أول 24 ساعة بعد الإطلاق

- راقب فشل Auth، أخطاء PostgREST وStorage، زمن الصفحة الرئيسية ونسبة نشر الإعلانات الفاشلة.
- راجع البلاغات وطلبات حذف الحساب كل ساعتين أثناء ساعات التشغيل.
- لا تنفذ تعديل مخطط جديد إلا لإصلاح P0/P1 وبخطة تراجع.

## أول 7 أيام

- راجع الأخطاء المتكررة وأحجام الصور والطلبات البطيئة يوميًا.
- راجع صلاحيات الإدارة وطلبات حذف الحساب والبلاغات.
- نفّذ مراجعة أسبوعية للنسخ الاحتياطي، نتائج GitHub Actions وأخطاء 404.

## سجل الحادث

سجّل: وقت الاكتشاف، الخطورة، الخدمات المتأثرة، صاحب القرار، خطوات الاحتواء، وقت الاستعادة، أثر المستخدمين، الحاجة لإشعار نظامي، والإجراء الوقائي. لا تضع أسرارًا أو بيانات شخصية في GitHub Issues.
